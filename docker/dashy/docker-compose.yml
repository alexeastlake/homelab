services:
  dashy:
    image: lissy93/dashy
    container_name: dashy
    volumes:
      - ./conf.yml:/app/user-data/conf.yml
    ports:
      - 8081:8080
    environment:
      - NODE_ENV=production
    restart: unless-stopped